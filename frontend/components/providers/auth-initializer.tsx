"use client";

import { useEffect } from "react";
import { MOCK_AUTH_DATA } from "@/lib/constants/mockAuth";

export function AuthInitializer() {
  useEffect(() => {
    // Mock auth removed - use actual authentication
  }, []);

  return null;
}
